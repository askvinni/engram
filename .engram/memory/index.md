# Engram Memory Index

Agents: read this index to find relevant learned docs. Load individual files only when their "Read when" condition matches your current task.

| File | Title | Read when |
|------|-------|-----------|
| @.engram/memory/patterns/batch-branch-creation-return-to-base.md | Capture and return to the base branch between iterations in batch branch workflows | implementing a batch command that creates one branch per issue; adding a new --all variant to any engram command that calls cmd_learn internally; debugging unexpectedly stacked engram/learn-N branches |
| @.engram/memory/patterns/branch-naming-conventions-for-engram-land-cleanup.md | engram land only auto-deletes branches named fix/issue-N, feat/issue-N, or issue-N | creating a local branch to work on an engram-tracked issue; implementing or changing the branch-deletion step in cmd_land |
| @.engram/memory/patterns/bumpers-not-blocks-validation.md | engram validation emits stderr warnings but proceeds — bumpers, not hard blocks | adding input validation to any engram command; deciding whether a validation failure in cmd_plan or a similar command should abort or warn |
| @.engram/memory/patterns/cmd-status-branch-issue-number-heuristic.md | cmd_status extracts issue numbers from branches via first-integer scan, unlike cmd_land's fixed patterns | creating a branch for an engram issue that should appear in engram status output; implementing or changing the branch-to-issue linking logic in cmd_status; debugging why engram status shows the wrong issue for a branch |
| @.engram/memory/patterns/gh-cli-bypass-helper-for-stderr-inspection.md | Bypass the gh() helper and use Command::new("gh").output() when a wrapper must be idempotent | adding a gh CLI wrapper in github.rs that must treat one specific failure mode as success rather than an error; implementing an idempotent GitHub operation that should no-op if the target is already in the desired state |
| @.engram/memory/patterns/gh-cli-json-fields-are-camelcase.md | gh CLI --json output uses camelCase field names, not snake_case | adding a new struct to deserialize gh --json output in github.rs; adding a new gh issue or gh pr JSON query and mapping fields to a Rust struct |
| @.engram/memory/patterns/graphql-closed-event-for-linked-pr.md | Use GraphQL CLOSED_EVENT to find the PR that closed an issue — text search has post-merge lag | implementing or changing find_linked_pr() in github.rs; looking up which PR closed a given GitHub issue from code |
| @.engram/memory/patterns/hidden-json-comment-in-issue-body.md | Store machine-readable state in hidden HTML comments within GitHub issue bodies | attaching structured data to a GitHub issue that must survive gh issue edit --body round-trips; designing a command that reads and writes structured state from a GitHub issue body; implementing a new engram command that updates issue body content programmatically |
| @.engram/memory/patterns/objective-plan-marker-links-plan-to-node.md | Plans linked to objective nodes carry an `Objective: #N (node id)` prefix that cmd_land reads to auto-update node status | creating a plan issue body that should auto-update an objective node when it lands; implementing or changing maybe_mark_node_done() in objective.rs; adding a new code path that creates engram-plan issues on behalf of an objective node |
| @.engram/memory/patterns/objective-roadmap-bullet-list-input-format.md | Supply bullet-list roadmap to `engram objective new` — CLI generates the table and hidden JSON, not the caller | creating an engram-objective GitHub issue from code or a skill; calling parse_nodes_from_roadmap_input in src/objective.rs; debugging why --all-unblocked does not dispatch nodes on a newly created objective |
| @.engram/memory/patterns/prompt-hooks-scope-learn-only.md | Prompt hooks in .engram/prompt-hooks/ are injected only during engram learn, not other commands | writing a prompt hook rule and expecting it to affect engram plan or other Claude-invocation commands; adding a new Claude synthesis function in claude.rs and deciding whether to inject prompt hooks; debugging why a prompt hook has no visible effect on a particular engram command |
| @.engram/memory/patterns/secondary-github-api-calls-are-non-fatal.md | Treat secondary GitHub API calls (sub-issue linking, label decoration) as non-fatal — warn and continue | adding a secondary GitHub API call after a primary issue or PR creation; deciding whether a GitHub helper step failure should abort an engram command |
| @.engram/memory/tripwires/doctor-claude-check-does-not-verify-noninteractive-mode.md | engram doctor's claude check verifies installation only, not non-interactive usability | debugging why engram learn fails after engram doctor reports all checks passing; adding a new check to cmd_doctor that exercises a dependency beyond binary presence |
| @.engram/memory/tripwires/engram-list-requires-engram-plan-label.md | engram list only surfaces issues that carry the engram-plan label | creating a GitHub issue that should appear in engram list output; debugging why an expected plan issue does not appear in engram list |
| @.engram/memory/tripwires/find-pr-for-branch-open-prs-only.md | find_pr_for_branch returns None for merged or closed PRs — gh pr list defaults to open only | calling find_pr_for_branch to look up whether a branch has an associated PR; implementing or extending cmd_status or any command that shows PR state for a branch; debugging why engram status shows 'PR: none' on a branch that had a merged PR |
| @.engram/memory/tripwires/github-no-negative-label-filter.md | GitHub API has no native 'does not have label' filter — client-side post-filtering required | querying GitHub issues or PRs that lack a specific label; implementing or changing list_unlearned_plans(); adding a new GitHub query that needs to exclude issues by label |
| @.engram/memory/tripwires/globbing-a-prompt-hooks-directory-loads-readme-md-as-a-rule-.md | Globbing a prompt-hooks directory loads README.md as a rule file unless excluded | loading files from a directory that may contain documentation files; implementing or changing load_prompt_hooks(); adding a new directory where users place configuration files alongside a README |
| @.engram/memory/tripwires/graphql-mutations-require-node-ids-not-issue-numbers.md | GitHub GraphQL mutations accept opaque node IDs, not integer issue numbers | implementing a GitHub GraphQL mutation in github.rs that references issues or PRs; adding a new function that calls addSubIssue, addLabelsToLabelable, or any GitHub mutation with an ID! parameter |
| @.engram/memory/tripwires/inlining-memory-content-into-claude-md-is-unsafe-if-any-lear.md | Inlining memory content into CLAUDE.md corrupts section boundaries if content contains the end marker | writing memory content into CLAUDE.md; implementing or changing write_claude_md_section(); changing the engram section delimiter strings |
| @.engram/memory/tripwires/invoking-claude-p-from-within-a-repo-directory-causes-claude.md | Invoking claude -p from inside a repo directory loads CLAUDE.md as agent context | calling claude -p programmatically from engram; implementing or changing synthesize_learnings() or any function that shells out to Claude; debugging unexpected Claude behavior where it acts as an agent instead of returning JSON |
| @.engram/memory/tripwires/objective-dag-cycle-breaks-dispatch-silently.md | Cycles in an objective node DAG silently break dispatch and auto-close at runtime, not at creation time | creating an engram-objective issue whose nodes have depends_on relationships; editing or adding dependency edges to an existing objective's node graph; implementing code that modifies the engram:nodes JSON on an objective issue |
| @.engram/memory/architecture/memory-index-self-routing.md | CLAUDE.md references only index.md; agents self-route to individual topic files via read_when | modifying what engram writes into CLAUDE.md; implementing or changing write_topic_file() or rebuild_index() in memory.rs; debugging why a memory topic file is not being loaded by agents |
